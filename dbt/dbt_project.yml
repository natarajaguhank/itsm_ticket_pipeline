name: itsm_dbt_project
version: '1.0'
config-version: 2

profile: default

target-path: target
clean-targets:
  - target
  - dbt_modules

models:
  itsm_dbt_project:
    staging:
      +materialized: table
    marts:
      +materialized: table
